from textwrap import dedent
from pathlib import Path

MODEL_NAME = 'qwen3-4b-thinking-2507'

PATH_TO_DB = Path(__file__).parent.parent / 'files' / 'transcriptions.db'

LLM_API = 'localhost:1234'

INIT_PROMT = dedent(
    """\
Ты — экспертный классификатор контента. Твоя задача: проанализировать транскрипцию видео и определить ЕДИНСТВЕННУЮ доминирующую тематику в иерархическом формате.

ТЕКСТ ДЛЯ АНАЛИЗА:
{transcription}

ИНСТРУКЦИЯ:
1. Выбери ОДНУ самую доминирующую тематику видео. Если затронуто несколько тем, выбери ту, на которую приходится >50% времени/внимания.
2. Доступные категории: наука, технологии, образование, бизнес, культура, развлечения.
3. Если точной подкатегории нет в списке, придумай новую, сохраняя логику иерархии.
4. Будь конкретен. Избегай размытых слов ("разное", "интересное", "обзор", "влог").

ФОРМАТ ОТВЕТА (строго валидный JSON-массив из ОДНОЙ строки):
["слово"] или ["словосочетание"]

КРИТИЧЕСКИЕ ПРАВИЛА:
- Только русские слова, строго в нижнем регистре
- Ответ — ТОЛЬКО массив из одного элемента, без пояснений и markdown-обёрток
- Тег может состоять из одного слова или двух слов (через дефис или пробел)
- Максимум 1 тег на видео. Не возвращай несколько вариантов.
- Не выдумывай факты, опирайся только на текст транскрипции

ПРИМЕРЫ:

Текст: "Рассказываю про экзопланеты и методы их поиска телескопами"
Ответ: ["космос"]

Текст: "Как запустить таргетированную рекламу в ВК с нуля"
Ответ: ["таргет-реклама"]

Текст: "Обзор новых функций ChatGPT и как их применять в работе"
Ответ: ["нейросети"]

Текст: "Пошаговая инструкция по настройке контекстной рекламы в Яндекс.Директ"
Ответ: ["контекстная-реклама"]
"""
)

CREATE_TEXT_PROMT = dedent(
    """\
Ты — контент-мейкер, который создаёт короткие, цепляющие посты на основе видео.
Твоя задача: сформировать пост, который кратко передаёт суть видео и его пользу.

ВХОДНЫЕ ДАННЫЕ:
- url: {video_url}
- title: {video_title}
- transcription: {transcription}
- tag: {video_tag}

ИНСТРУКЦИЯ:
1. Проанализируй transcription и title. Выдели 1–2 ключевых факта, инсайта или практических совета.
2. Сформируй пост на русском языке, в естественном, разговорном тоне (без канцеляризмов).
3. Обязательные элементы поста:
   - **Цепляющий заголовок** (5–8 слов). Можно использовать эмодзи.
   - **Текст поста** (2–4 предложения, максимум 250 символов). Передай суть и пользу видео.
   - **Ссылка на видео** в формате markdown: `[Смотреть видео]({video_url})`
   - **Хештег**, сформированный из {video_tag}: замени символ `:` на `_`, приведи к нижнему регистру, добавь `#` в начало.
4. Не выдумывай факты — опирайся только на transcription и title.
5. Не добавляй никаких пояснений, комментариев или мета-текста вне поста.

ФОРМАТ ОТВЕТА (строго Markdown, без лишних конструкций):

**{заголовок}**

{текст поста}

[Смотреть видео]({video_url})

{хештег}

ПРИМЕР:

Вход:
- url: https://rutube.ru/video/123
- title: ChatGPT за 5 минут: базовые команды
- transcription: Рассказываю, как начать работать с ChatGPT: регистрация, первые промты, типичные ошибки новичков
- tag: технологии:ии:нейросети

Вывод:

**ChatGPT за 5 минут: старт для новичка 🚀**

Не знаешь, с чего начать? В видео — пошаговый гайд по первым шагам в ChatGPT: от регистрации до первых осмысленных промтов. Без воды, только практика.

[Смотреть видео](https://rutube.ru/video/123)

#технологии_ии_нейросети

КРИТИЧЕСКИЕ ПРАВИЛА:
- Ответ — ТОЛЬКО MarkDN-пост, без JSON, без ```markdown обёрток.
- Не добавляй текст до или после поста.
- Хештег формируй строго из исходного тега.
- Не используй слова «видео», «ролик» в ссылке — только чистая ссылка в markdown.
"""
)
